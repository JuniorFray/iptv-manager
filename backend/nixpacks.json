{
  "providers": [
    "node"
  ],
  "nodejs": {
    "version": "20"
  },
  "phases": {
    "install": {
      "cmds": [
        "npm install --legacy-peer-deps"
      ]
    },
    "build": {
      "cmds": [
        "npx playwright install chromium --with-deps"
      ]
    }
  }
}